jdk:
  - openjdk11

before_install:
  - ./gradlew clean

install:
  - ./gradlew -x test build

before_script:
  - ./gradlew clean

script:
  - ./gradlew publishToMavenLocal

# Clear Gradle cache to avoid lint-model issues
after_script:
  - rm -rf ~/.gradle/caches/